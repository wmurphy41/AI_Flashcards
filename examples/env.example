# ===============================
# AI Flashcards — Environment Variables Template
# ===============================
#
# This file is a TEMPLATE for environment variables needed by the application.
#
# -------------------------------
# Setup Instructions
# -------------------------------
# 1. Copy this file to create your actual .env file:
#    cp examples/env.example .env
#
# 2. Fill in the actual values (never commit .env to git)
#
# 3. Secure the file:
#    chmod 600 .env
#
# 4. Use with Docker Compose:
#    docker compose -f docker-compose.prod.yml --env-file .env up
#
# ===============================

# -------------------------------
# OpenAI API Configuration
# -------------------------------
# Required for AI deck generation functionality.
# Get your API key from: https://platform.openai.com/api-keys
#
# Format: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Example: OPENAI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
OPENAI_API_KEY=sk-your-key-here

# -------------------------------
# Frontend Build Configuration
# -------------------------------
# These variables are used during the frontend build process (npm run build).
# They are baked into the static files at build time, not read at runtime.
#
# IMPORTANT: If you change these values, you must rebuild the frontend image.

# -------------------------------
# Option 1: Root Path Deployment (Default)
# -------------------------------
# Use these values when deploying the app to the root of your domain:
#   - https://example.com/
#   - https://example.com/decks
#   - https://example.com/api/health
#
# Base path for the frontend application
# Set to "/" for root deployment
VITE_BASE_PATH=/

# API base path (must match your nginx/server routing)
# Set to "/api" for root deployment
VITE_API_BASE=/api

# -------------------------------
# Option 2: Subdirectory Deployment
# -------------------------------
# Use these values when deploying to a subdirectory:
#   - https://example.com/flashcards/
#   - https://example.com/flashcards/decks
#   - https://example.com/flashcards/api/health
#
# Uncomment and use these values instead of the root path values above:
# VITE_BASE_PATH=/flashcards/
# VITE_API_BASE=/flashcards/api

# -------------------------------
# Development/Proxy Configuration
# -------------------------------
# Used by Vite dev server for local development.
# Not needed in production (handled by nginx).
#
# VITE_API_PROXY_TARGET=http://localhost:8000

# ===============================
# Notes
# ===============================
#
# 1. VITE_* variables are only read during build time (npm run build).
#    Changing them requires rebuilding the Docker image.
#
# 2. OPENAI_API_KEY is read at runtime by the backend container.
#    It can be changed without rebuilding.
#
# 3. For Docker Compose, environment variables can be set in:
#    - .env file (recommended for secrets)
#    - docker-compose.yml environment section
#    - Command line: docker compose --env-file .env up
#
# 4. Never commit .env files containing real secrets to git.
#    The .gitignore file already excludes .env files.
